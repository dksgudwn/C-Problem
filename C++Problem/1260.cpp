#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> isLinked;
vector<vector<int>> visited;

void DFS(int v, int m)
{
	for (int i = 0; i < m; i++)
	{

	}
}

int main()
{
	int N, M, V;
	cin >> N >> M >> V;

	DFS(V, M);
}